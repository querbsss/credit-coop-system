# Environment Variables Template for Credit Cooperative System on Render

## Database Service (Automatically handled by Render)
# DATABASE_URL=postgresql://username:password@host:port/database

## Backend API Service
NODE_ENV=production
PORT=10000
# DATABASE_URL will be automatically injected from the database service

## Landing Page Service  
NODE_ENV=production
PORT=10000
# DATABASE_URL will be automatically injected from the database service

## Member Portal Backend
NODE_ENV=production
PORT=10000
# DATABASE_URL will be automatically injected from the database service
JWT_SECRET=your-super-secure-jwt-secret-key-here
BCRYPT_ROUNDS=12

## Member Portal Frontend
NODE_ENV=production
PORT=10000
REACT_APP_API_URL=https://credit-coop-member-backend.onrender.com

## Staff Portal Backend
NODE_ENV=production
PORT=10000
# DATABASE_URL will be automatically injected from the database service
JWT_SECRET=your-super-secure-jwt-secret-key-here
BCRYPT_ROUNDS=12

## Staff Portal Frontend
NODE_ENV=production
PORT=10000
REACT_APP_API_URL=https://credit-coop-staff-backend.onrender.com

## Additional Environment Variables (Optional)
# Add these if needed for specific features:

# Email configuration (if using email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# File upload configuration
# MAX_FILE_SIZE=5242880
# UPLOAD_PATH=/tmp/uploads

# Session configuration
# SESSION_SECRET=your-session-secret-key

# CORS origins (additional domains if needed)
# CORS_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

## Instructions:
# 1. The DATABASE_URL will be automatically set by Render when you create a PostgreSQL database
# 2. JWT_SECRET should be a strong, random string - Render can generate this for you
# 3. Update the REACT_APP_API_URL values to match your actual Render service URLs
# 4. Add any additional environment variables your application needs